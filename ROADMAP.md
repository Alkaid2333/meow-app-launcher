# 路线图

meow-app-launcher 的分阶段开发规划喵。当前版本 **v1.6.0**，处于第三阶段 a 的收尾段喵。

已经交付的内容见 [CHANGELOG.md](CHANGELOG.md)喵。

---

## ✅ 已完成

### Phase 1 — Windows 基础功能闭环

透明异形置顶窗口 + Skia 自绘的灵动岛，跑通「呼出 → 搜索 → 启动」全链路喵。

- 全局热键呼出、弹簧呼出/展开动画、停稳即退出帧循环喵
- 英 / 中 / 拼音模糊搜索，名称 / `t:` 标签 / `i:` 首字母三种模式喵
- 开始菜单 `.lnk` 扫描 + 图标提取与 PNG 快照缓存喵
- 键盘导航、回车启动、点击结果启动、Esc 隐藏喵
- 配置与注册表 JSON 持久化、主题、logforth 彩色日志喵

### Phase 2 — 完整桌面体验

从「能用」到「像个桌面软件」喵。

- 多窗口架构 + 命令队列协调（启动器兼任应用控制器）喵
- 系统托盘（主题自适应图标 + 显示/隐藏、设置、重启、退出菜单）喵
- 配置 GUI：侧边栏 + 分组卡片，可视化配置主题 / 窗口 / 热键喵
- 中文输入法（IME 组字预览 + 提交）喵
- 手动注册应用（CLI + 拖拽）喵
- 滑动毛玻璃 / 云母材质、最近使用 / 收藏 / 最常用统计、首字母分组与 Tag 管理喵
- 开机自启、CPU / GPU 双渲染后端（失败自动回退）、热键实时生效喵
- 单实例保护、托盘图标随 Explorer 重启重建、前台焦点硬化喵

---

## 🚧 进行中

### Phase 3a — 搜索中枢

把「搜索结果」从一个应用列表，变成一条可以挂载任意来源的管道喵。

**已落地喵**

- [x] `SearchItem` / `Action` 统一结果与动作模型喵
- [x] `SearchProvider` trait + 内置源（计算器 / Web 搜索 / 系统命令）喵
- [x] 多源聚合与分数域约定喵
- [x] 内置计算器（shunting-yard，回车复制结果）喵
- [x] Web 搜索跳转（引擎可配）+ 自定义浏览器（`%1` 占位符）喵
- [x] 系统命令（锁屏 / 睡眠 / 关机 / 重启）+ 可配置别名表喵
- [x] 单实例互斥 + 二次启动唤起已有实例喵
- [x] 统一图标渲染层（viewBox / 线宽填充混排 / 标准内边距）喵
- [x] 新配置进入配置 GUI（Web 搜索 + 指令模块两组）喵

**余下喵**

- [ ] **named-pipe IPC** —— `meowal query` / `meowal show` / `meowal hide` 完整 CLI 联动喵。
      目前是 `Mutex` + `FindWindow` + `PostMessage` 的简化版，只能唤起、不能传参喵。
- [ ] **设置 GUI 的 `RowDesc` 数据驱动化** —— 当前每新增一个配置项要在 GUI 侧改 4 处、涉及 2 个文件喵。
      数据驱动化后应降到 1 处喵。

---

## 🔭 计划中

### Phase 3b — 文件搜索与预览

| 项目 | 方案喵 |
|---|---|
| 文件搜索 | **集成 Everything** —— 通过 `es.exe` 或 SDK DLL 接入，在 `Platform` 层抽象出 `FileIndexProvider` 喵 |
| 文件预览 | QuickLook 式悬浮预览，首期覆盖文本 / 图片 / PDF 喵 |

> **为什么不自研索引喵**：自研 MFT / USN Journal 方案需要管理员权限，工程量也远超收益，
> 已在 2026-09-05 被否决喵。

### Phase 4 — 岛常驻与媒体（v2.0）

- **常驻 pin 态** —— 让岛可以固定在桌面一角，而不只是呼出即用喵。
- **SMTC 媒体控制** —— 读取并控制系统正在播放的媒体喵。
  ⚠️ 这需要引入 `windows` crate 的 WinRT 绑定（`windows-sys` 不含 WinRT），**届时需要单独拍板**喵。
- **日历与时钟** —— 岛上的时间信息喵。

### Phase 5 — 插件体系

- 先做**内置 Provider trait**（地基已在 Phase 3a 立好），外部插件形态（Rhai / WASM）等接口验证后再议喵。
- **通知拦截** 暂时观望 —— 相关 API 仍不稳定喵。

### Phase 6 — 跨平台

- 评估 Linux / macOS 后端喵。
- 架构上已为此准备：`animation/` 与 `render/` 不含平台代码，新增后端是实现 `Platform` trait，而不是重写喵。

---

## 📌 已拍板的技术决策

记录于 2026-09-05，避免后续反复摇摆喵。

| # | 决策喵 | 理由喵 |
|---|---|---|
| D1 | **搜索中枢拓展**作为 Phase 3 主线 | 结果模型与动作模型的缺失（R1 / R2）是后续一切功能的瓶颈喵 |
| D2 | 文件搜索**集成 Everything**，不自研索引 | 自研 MFT / USN 需要管理员权限且工程量过大喵 |
| D3 | 插件体系**内置 Provider trait 先行** | 先验证接口形状，再决定外部插件形态喵 |
| D4 | 渲染**维持 GL + CPU**，不投入 Vulkan | 岛体负载很轻，真正的瓶颈是 present 时的读回；且受 Skia 二进制资产约束喵 |
| D5 | 配置 GUI 随 Phase 3a 做 **`RowDesc` 数据驱动化** | 把新配置项的成本从 4 处降到 1 处喵 |

## 📐 设计契约

跨阶段生效的约定，改动前请先想清楚喵。

- **分数域** —— 计算器 `3.0` 置顶 → 系统命令 `模糊分 + 2.0` → 应用原始模糊分 → Web `0.0` 垫底；
  同分按标题稳定排序，统一 `MAX_RESULTS = 30` 截断喵。
- **定向模式不注入内置源** —— `t:` / `i:` 只搜应用，避免结果被噪声污染喵。
- **模块边界** —— `platform/` 是唯一出现 `cfg(target_os)` 的目录；
  `animation/` 是纯数学，不依赖 Skia 与平台喵。
- **测试守门** —— 目前 66 个测试全绿是每次改动的门禁喵。

## ⚠️ 已知约束

| 约束喵 | 影响喵 |
|---|---|
| 需要预编译 Skia 二进制 | 本机无 LLVM，无法从源码编译 Skia；构建依赖 `binary-cache` 或本地 `file://` 缓存喵 |
| 仅 Windows 后端 | 其他平台需实现 `Platform` trait 喵 |
| 界面文案为中文 | 国际化尚未排期喵 |
